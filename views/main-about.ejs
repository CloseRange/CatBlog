<%- include("partials/head") %>
<%- include("partials/nav") %>

<main id="main-content" class="main-shell">
  <section class="main-card reveal-up">
    <p class="main-eyebrow">About</p>
    <h1>About The CatBlog Directory</h1>
    <% (String(directoryAbout || "").split(/\n\s*\n/).filter(Boolean)).forEach((paragraph) => { %>
      <p><%= paragraph %></p>
    <% }) %>
  </section>
</main>

<%- include("partials/footer") %>
